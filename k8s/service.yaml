apiVersion: v1
kind: Service
metadata:
  name: password-manager-service
spec:
  selector:
    app: password-manager
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort
